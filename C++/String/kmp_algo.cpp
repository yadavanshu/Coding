#include <bits/stdc++.h>
using namespace std;

void countLPS(string str, string pat, vector<int> &lps){
    
}

int kmp(string str, string pat)
{
    int n = str.length();
    int m = pat.length();
    vector<int> lps(m);
    int i = 0, j = 0;

    return -1;
}

int main()
{
    string str;
    string pat;
    getline(cin, str);
    getline(cin, pat);
    cout << kmp(str, pat);

    return 0;
}